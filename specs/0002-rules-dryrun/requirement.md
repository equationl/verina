## 规则编辑页面支持测试

1. 在编辑规则时，保存按钮旁边添加一个测试：只有所有的必填项都填写了，例如规则名称、以及至少配置了一个动作，才可以点击

2. 点击测试后，自动为这些可插值变量生成 mock 的数据，然后执行动作；弹出一个浮层展示测试的结果，即 webhook 调用后的返回（状态码、headers、body 等）。

## 支持多种触发动作

触发动作不只局限于 webhook，可以由用户选择，然后渲染不同的表单。只不过目前先只有 webhook 一个选项。

注意：数据结构如果变更，要向前兼容，不要让用户更新之后就闪退。